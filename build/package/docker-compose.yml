services:
  go-auth-yourself:
    container_name: go-auth-yourself
    build: ./
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ../../:/app
  simple-app:
    container_name: simple-app
    hostname: simple-app
    image: httpd
    ports:
      - '8081:80'
